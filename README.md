## Neovim config

### Plugin Management
This config uses Neovim 0.12's native `vim.pack` for plugin management. On first launch, you'll be prompted to install all plugins -- press `a` to accept all. The lockfile (`nvim-pack-lock.json`) is tracked in git for reproducibility.
